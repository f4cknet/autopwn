# [v5.0.1] 治理变更实施 + 文档落盘

- 状态：✅
- Owner：@Minzhi_Zhou
- 立项日期：2026-08-30
- 完成日期：2026-08-30
- 索引位置：[`upgraded.md §3.1`](../upgraded.md)

## 1. 背景

Owner 已确认：v4 不再作为当前规范，统一归档；v5 需要新的需求、架构与任务入口。

## 2. 目标

1. 建立 `archive/v4/` 归档入口
2. 在 root 建立 `v5_prd.md` 与 `v5_architecture.md`
3. 把 `AGENTS.md` / `upgraded.md` 的默认阅读入口切到 v5
4. 保留 v4 基线与历史任务的追溯能力

## 3. 范围

- `AGENTS.md`
- `README.md`
- `bugs/fix.md`
- `upgraded.md`
- `upgraded/v5.0.1.md`
- `v5_prd.md`
- `v5_architecture.md`
- `archive/v4/*`

## 4. 非目标

- 不改 runtime 代码
- 不改现有 exploit 行为
- 不处理 `Challenge/chall`

## 5. 风险

- 只迁旧文档、不落新入口，会造成治理断档
- archive 结构不稳定，会导致 v4 详情链接失效

## 6. 验收

1. push 到 `main`
2. `pytest tests/unit -m "not integration" -q` 全过
3. root 默认规范入口切到 v5
4. `archive/v4/` 可追溯 v4 规范
5. Owner 自审
6. 同提交同步索引、详情、治理规则与归档入口

## 7. 完成记录

- 已建立 `archive/v4/`，并迁入 `refactor.md`、`rebuild.md`、原 `upgraded` 历史索引 / backlog / 状态 / `v4.1.x` 详情
- 已在 root 新建 `v5_prd.md` 与 `v5_architecture.md`
- 已更新 `AGENTS.md`，将默认阅读入口切换为 `upgraded.md` + `v5_prd.md` + `v5_architecture.md`
- 已更新 `upgraded.md`，将当前主索引切换到 v5，并把 v4 历史入口下沉到 `archive/v4/`
- 已更新 `README.md` 与 `bugs/fix.md`，修正当前开发入口与 archive 链接
- 验证结果：`docker exec ctf_env /bin/bash -lc "cd /data/autopwn && ./.venv/bin/python -m pytest tests/unit -m 'not integration' -q"` → `757 passed in 26.74s`
