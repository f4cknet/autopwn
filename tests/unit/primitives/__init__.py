"""Marker file for the ``tests/unit/primitives`` sub-package.

This sub-package holds the shared test helpers
(``_common.py``) and cross-primitive contract tests
(``test_primitives_contract.py`` + ``test_primitives_helper_errors.py``).
The per-primitive tests (``test_primitives_*.py``) live in
the parent ``tests/unit/`` directory because they were created
before the sub-package was introduced.
"""
