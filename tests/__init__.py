"""AutoPwn test suite (P5.5 + P9).

See ``rebuild.md`` §4.10 P9 for the full test roadmap.  P5.5
is the first PR to introduce tests (per-module unit tests for
the 4 detect modules); the broader P9 work (integration tests,
CI, coverage gates) is scheduled for later.
"""
