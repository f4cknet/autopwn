# [v5.0.4] 能力层基础层
- 状态：✅
- Owner：@Minzhi_Zhou
- 立项日期：2026-08-30
- 完成日期：2026-08-30
- 索引位置：[`upgraded.md §3.3`](../upgraded.md)
## 1. 背景 / 目标 / 范围
`v5.0.2` 已落地 facts，`v5.0.3` 已落地 interaction graph；`v5.0.4` 继续把“路线为什么可执行”抽象为 capability，并只覆盖 same-session canary 与 2-stage ret2libc-write 两条已验证路线，不引入 planner / verifier 主体迁移，也不改 `Challenge/chall`。
## 2. 风险 / 6 关验收
- 风险：capability 不能退化成字符串重命名，必须绑定 `FactScope`、`InteractionGraph` 与 evidence。
- 6 关验收：push 到 `main`；`pytest tests/unit -m "not integration" -q` 全过；补 `Challenge/` 验证；复测 `canary` 与 `level3_x64`；Owner 自审；同提交同步索引 / 详情 / 架构 / 需求文档。
## 3. 实施
- `autopwn/context.py`：新增 `CapabilityKind / CapabilityPrereq / CapabilityBinding / CapabilityEvidence / Capability / make_capability()`，并为 `ExploitContext` 增加 `set_capability()` / `get_capability()`。
- `autopwn/exp/registry.py`：收集 strategy capability，注册到 binary scope，并在 `candidate order` 日志输出 capability 链。
- `autopwn/exp/strategies/{canary_ret2libc_put,ret2libc_write_x64}.py`：为 same-session canary 与 2-stage ret2libc-write 接入 capability 描述。
- `tests/unit/test_exp_registry.py`：补 capability 收集、记录与上下文注册断言。
## 4. 验证 / 收尾
- `docker exec ctf_env /bin/bash -lc "cd /data/autopwn && ./.venv/bin/python -m pytest tests/unit/test_exp_registry.py tests/unit/test_exp_canary.py tests/unit/test_exp_ret2libc_write.py -q"` → `124 passed`
- `docker exec ctf_env /bin/bash -lc "cd /data/autopwn && ./.venv/bin/python -m pytest tests/unit -m 'not integration' -q"` → `763 passed`
- `Challenge/canary`：`RC=0`，`whoami` 返回 `root`，并输出 capability 链 `same_session.canary_leak -> same_session.stack_control -> same_session.libc_puts_leak -> same_session.ret2libc_exec -> same_session.shell_verify_whoami`
- `Challenge/level3_x64`：`RC=0`，`ID_OUTPUT='PWNED\n'`，并输出 capability 链 `ret2libc_write.stack_control -> ret2libc_write.libc_leak -> ret2libc_write.shell_exec -> ret2libc_write.shell_verify`
- 2026-08-30：按单 Owner / 单目录流程直接 `commit + push` 到 `main`。
